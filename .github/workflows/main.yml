on: [push]

jobs:
  testing-job:
    name: A job testing the Github Actions workflow
    runs-on: ubuntu-latest
    steps:

    - name: Install s3cmd
      run: sudo apt-get install s3cmd

    - name: Run s3cmd
      run: s3cmd --help

